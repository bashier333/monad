# Data-corruption response (P-300)

1. Quarantine: disable the suspect rule/import path (toggle, don't delete — trail matters).
2. Scope: which orgs/weeks/figures changed (adjustments list + access logs).
3. Postmortem per affected figure (wrong-answer template).
4. Fix: correction, rule change, or code + fixture + eval case.
5. Tell affected pilots within 24h with the corrected numbers.
