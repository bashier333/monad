# Alerting (B-102)

No pager in beta — the founder is the pager. Wire these before pilot 1:

| Signal | How | Target |
|---|---|---|
| App down / 5xx spike | UptimeRobot (free) on `/api/health`, 5-min checks | SMS/email <5 min |
| FAILED imports | Daily review of `/api/admin/failures` + UptimeRobot keyword check | <24h |
| Brief not generated by Mon 8am | Calendar reminder until cron lands, then cron monitor | Monday AM |
| Past-due subscriptions | Weekly `/api/admin/costs` + Stripe dashboard | Monday review |
| Error-rate spike | Sentry (DSN pending) → email alerts | <1h |

Test each alert once by triggering it (bad deploy to staging, forced FAILED import,
test past-due in Stripe test mode). Record test dates here.
