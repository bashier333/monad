# 5-minute dispatcher walkthrough (P-320) — script

0-1m: the Monday question; what this replaces (spreadsheet hell).
1-2m: upload + mapping confirm on a real export.
2-3.5m: read the answer; drill one losing lane to its source rows.
3.5-4.5m: flag, approve, rule-ify; re-run and show the diff.
4.5-5m: brief, share link, invite the owner. CTA: pilot agreement link.
Record with a real dispatcher co-hosting after pilot 1. Transcript ships with captions.
