# Win-back sequence (P-277)

Automated sends activate with Resend live; until then run manually from the canceled list
(finance export + subscription status).

- Day 30: "Your lanes, 30 days later" — one insight from their last brief + resubscribe link.
- Day 60: what shipped since they left (changelog highlights relevant to their stalls).
- Day 90 (last): export reminder + "we delete on day 90 unless you return" + resubscribe link.
- Every send offers pause-instead-of-cancel. Every reply goes to a human in 4h.
